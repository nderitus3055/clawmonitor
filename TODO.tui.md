# TUI TODO

## Candidate Improvements

- Add expand/collapse for the currently selected history item so long summaries do not consume multiple rows by default.
- Add a dedicated fullscreen history mode separate from generic detail fullscreen if the current `Z` behavior still feels too broad.
- Add an even more compact footer mode for very narrow terminals.
- Consider a compact/full session list toggle if operators want a denser left pane even on wide monitors.
- Consider a small legend banner for `USER / ASST / RUN / FLAGS` near the top of the list.
- Consider a "sticky current task" line in history mode so active runs remain visible while scrolling older items.
