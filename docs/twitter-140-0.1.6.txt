ClawMonitor v0.1.6: OpenClaw session monitor (TUI/CLI). New Focus mode (hide stale noise), in-TUI rename/labels for opaque IDs (Feishu ou_/Telegram chats), cron job view, clearer flags/logs. `pip install clawmonitor` + `clawmonitor tui`.
